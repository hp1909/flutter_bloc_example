name: manabie_code_challenge
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.2
  equatable: ^0.2.6
  uuid: ^2.0.1
  flutter_bloc: ^0.15.1
  get_it: ^1.0.3+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  test: any
  flutter_driver:
    sdk: flutter
  mockito: ^4.1.0

flutter:
  uses-material-design: true
